# OpenCLI Chrome 数据目录（留空使用默认）
OPENCLI_CHROME_DATA_DIR=

# 数据库配置
DB_TYPE=sqlite
DB_PATH=./data/app.db

# 服务端口
PORT=3001

# 调度配置（cron 表达式）
COLLECT_CRON=0 8,14,20 * * *
PROCESS_CRON=30 8,14,20 * * *

# 数据保留天数（时效性过滤）
DATA_RETENTION_DAYS=90